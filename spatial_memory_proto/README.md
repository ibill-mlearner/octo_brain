# Spatial Memory Prototype

Run:

```bash
cd spatial_memory_proto
python demo.py
```

This runs a single-process demo with four node roles (sensor/reflex/decision/actor) sharing one spatial memory core.
